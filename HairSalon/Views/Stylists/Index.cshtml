@{
  Layout = "_Layout";
}

@using HairSalon.Models

@foreach (Stylist stylist in Model)
{
  <p>@Html.ActionLink($"{stylist.Name}'s Details", "Details", new { id = stylist.StylistId})</p>
}


<p>@Html.ActionLink("Add a Stylist", "Create", "Stylists")</p>